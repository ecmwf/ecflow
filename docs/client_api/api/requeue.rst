
.. _requeue_cli:

requeue
*******






.. rubric:: Output of :code:`--help=requeue`



The following help text is generated by :code:`ecflow_client --help=requeue`

::

   
   requeue
   -------
   
   Re-queues the specified node(s)
   
   This command restores the specified node(s), and recursively all their children,
   to the state their default state. This is a comprehensive reset of the following:
   
     * the specified node(s) state
       - the value is taken from the default status (i.e. defstatus), when defined
       - otherwise, the state is set to `queued`
     * the suspended status of the children of the specified node(s)
       - n.b.: the suspended status of the specified node(s) themselves is *NOT* cleared
     * the job output counter (i.e. ECF_TRYNO)
       - the next run writes to <task>.1 and thus *overwrites* the output of the previous runs
     * the aborted reason, the job password, and the process identifier
     * the node flags
       - with the exception of the message and archived flags, which are preserved
     * repeat attributes
       - repeats are reset to their starting value
     * time based attributes
       - next time slot is re-evaluated, and relative durations are reset
     * the late flag, the events, and the meters
     * labels
       - *ONLY* those defined on suites and families, and *NOT* those on tasks
     * queue attributes
     * limits, together with any limit token held by the node(s) or their children
   
   Note that the enclosing suite must have been begun (see --begin) before any of its nodes
   can be re-queued. When the `force` mode is used, user zombies are created for any task
   that is active or submitted at the time the command is issued.
   
   To place a task back into the queued state while preserving the output of the previous
   runs, and without resetting repeats, time based attributes, and flags, use
   `--force=queued` instead.
   
   Argument(s):
   
       mode: (optional) [ abort | force ]
           # abort  = re-queue only aborted tasks below node
           # force  = Force the re-queueing even if there are nodes that are active or submitted
           # <null> = Checks if any tasks are in submitted or active states below the node,
           #          and if so does nothing. Otherwise, re-queues the node.
   
       node: path
           # A space separared list of node paths. Paths must begin with a leading '/' character
   
   Usage:
   
       --requeue=abort /suite/f1  # re-queue all aborted tasks of /suite/f1
       --requeue=force /suite/f1  # forcibly re-queue /suite/f1 and all its children.May cause zombies.
       --requeue=/s1/f1/t1 /s1/t2 # Re-queue node '/suite/f1/t1' and '/s1/t2'
   
   The client considers, for both user and task commands, the following environment variables:
   
     ECF_HOST <string> [mandatory*]
       The main server hostname; default value is 'localhost'
     ECF_PORT <int> [mandatory*]
       The main server port; default value is '3141'
     ECF_SSL <any> [optional*]
       Enable secure communication between client and server.
     ECF_HOSTFILE <string> [optional]
       File that lists alternate hosts to try, if connection to main host fails
     ECF_HOSTFILE_POLICY <string> [optional]
       The policy ('task' or 'all') to define which commands consider using alternate hosts.
   
   The options marked with (*) must be specified in order for the client to communicate
   with the server, either by setting the environment variables or by specifying the
   command line options.
   


.. seealso::

   :ref:`Protocol reference for RequeueNodeCmd <proto_RequeueNodeCmd>` — the request/reply
   payloads exchanged with the server and the C++ command class that implements this command.

