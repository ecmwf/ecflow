#!/bin/perl -n


if(/void ([\w_]+)::create /)
{
        $c=$1;
}

if( /(^.*)( [\w_]+CB)(, \(XtPointer\) this\);.*$)/ )
{
        $_  = $1;
        $_ .= "&";
        $_ .= $c;
        $_ .= "::";
        $_ .= $2;
        $_ .= $3;
        $_ .= "\n";
}

print $_;
