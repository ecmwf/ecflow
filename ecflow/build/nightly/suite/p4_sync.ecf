%include <head.h>
# line just after head.h ------------------------------------------
# ======================================================================
# Run LOCALLY
# ======================================================================
export WK=%WK%
cd $WK

if [[ %SET_TO_TEST_SCRIPT% = true ]] ; then
   echo "Test only"
else
   sh -x build/p4_sync.sh
fi

%include <tail.h>
