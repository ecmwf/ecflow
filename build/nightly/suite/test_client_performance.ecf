%include <head.h>

# ======================================================================
# Run LOCALLY
# ======================================================================
export WK=%WK%

if [[ %SET_TO_TEST_SCRIPT% = true ]] ; then
   echo "Test only"
else
   cd $WK;
      
   # Clear up
   rm -rf `hostname`.*
   
   Client/bin/%COMPILER_VERSION%/profile/tclient
   
   # Move gmon file to Client. Easier for eclipse 
   mv gmon.out Client/.
   
   # Clear up
   rm -rf `hostname`.*
fi

%include <tail.h>