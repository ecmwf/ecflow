Reason to move to ecflow 5.0.0
====================================

o Allow family tobe limited as weel as tasks

o Functionality for archive and restore.( an auto migrate). To cope with extra large suites

o much better identification of zombies
  - i. password missmatch, pid miss match, if user created zombies we record user command that gave rise to the zombie
  
o security: password protection
o security: User commands will now log hostanme as well as the user that issued the command

o Improved why command( when using time attributes)

o New query command, allows us to query event,meter and trigger status.

o New  Queue attribute

o Uses the latest boost version

o Old GUI(ecfowview) will be retired.

