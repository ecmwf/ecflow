%include <head.h>

# ======================================================================
# Run LOCALLY
# ======================================================================
export WK=%WK%
 
cd $WK

if [[ %SET_TO_TEST_SCRIPT% = true ]] ; then
   echo "Test only"
else
   /usr/local/apps/cppcheck/current/bin/cppcheck --platform=unix64 --enable=all --inconclusive --std=posix -DDEBUG=1 .   
fi

%include <tail.h>
