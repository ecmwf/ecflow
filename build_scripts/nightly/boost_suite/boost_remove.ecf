%include <head.h>

if [[ %SET_TO_TEST_SCRIPT% = true ]] ; then
   echo "Test only"
else
   # ==========================================================
   # Run as REMOTE_HOST. i.e %REMOTE_HOST:%
   # ==========================================================
   
   if [[ -e %BOOST_DIR% ]]; then
      cd %BOOST_DIR%
      rm -rf %BOOST_VERSION% || true ;
   else
      mkdir -p %BOOST_DIR%
   fi
fi

%include <tail.h>
